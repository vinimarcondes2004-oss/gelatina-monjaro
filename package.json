{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",

  "scripts": {
    "start": "node dist/index.js",
    "build": "pnpm run build:app",
    "build:app": "pnpm -r --if-present run build"
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "typecheck:libs": "tsc --build",
    "dev": "echo \"dev não configurado\""
  },

  "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$pnpm_config_user_agent\" in pnpm/*) ;; esac'",

  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
